[build]
builder = "NIXPACKS"

[deploy]
startCommand = "daphne mediconnect.asgi:application --bind 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
